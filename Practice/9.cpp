#include<iostream>
using namespace std;
int main() {

unsigned int a = -112;
cout << a;
}
//Unsigned Integers (often called "uints") are just like integers (whole numbers) but have the 
//property that they don't have a + or - sign associated with them. Thus they are always non-negative (zero or positive).
